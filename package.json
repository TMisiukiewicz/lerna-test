{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "run-ios": "lerna run --scope @myapp --stream --parallel run-ios"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "lerna": "^8.1.2",
    "typescript": "^5.3.3"
  }
}
