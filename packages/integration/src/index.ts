import { Doctor } from "@lerna-test/doctor";
import { Patient } from "@lerna-test/patient";
import { Scheduler } from "@lerna-test/scheduler";

console.log(new Doctor());
console.log(new Scheduler());
console.log(new Patient());
